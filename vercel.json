{
  "framework": "nextjs",
  "buildCommand": "cd apps/web && npm install && npm run build",
  "outputDirectory": "apps/web/.next",
  "installCommand": "echo 'skip root install'",
  "env": {
    "NEXT_PUBLIC_API_URL": "@sellsync_api_url"
  }
}
